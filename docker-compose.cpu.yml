services:
  indextts:
    build:
      context: .
      dockerfile: Dockerfile.cpu
    platform: linux/amd64
    ports:
      - "7861:7861"
    volumes:
      - ./checkpoints:/app/checkpoints
      - ./outputs:/app/outputs
    restart: unless-stopped
